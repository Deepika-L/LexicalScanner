int main()
{
  for (auto token : _tokens)
  {
    std::cout << token << " ";
  }

  for (size_t i = 0; i<length(); ++i)
    if (tok == _tokens[i])
      return i;

  for (int i=0, *p = &i; i < 9; i+=2) {
    std::cout << i << '\n';
  }
}